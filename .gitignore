# Ignore everything by default
*

# Allow these folders (the directory entries themselves)
!/src/
!/data/
!/notebooks/
!/img/

# Then allow everything under them (recursively)
!/src/**
!/data/**
!/notebooks/**
!/img/**

# Allow important files in repo root
!README.md
!requirements.txt

# (optional) keep .gitignore itself tracked
!.gitignore
img